name: Build All Platforms

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:
    inputs:
      version:
        description: 'Version (e.g. v1.0.0)'
        required: true

jobs:
  build-windows:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Install dependencies
        run: pip install pyinstaller pywebview requests
      - name: Build
        run: |
          cd gui
          pyinstaller --onefile --windowed --name "aikun-manager" main.py
      - uses: actions/upload-artifact@v4
        with:
          name: windows
          path: gui/dist/aikun-manager.exe

  build-macos:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Install dependencies
        run: pip install pyinstaller pywebview requests
      - name: Build
        run: |
          cd gui
          pyinstaller --onedir --windowed --name "aikun-manager" main.py
          cd dist
          mkdir -p "aikun-manager.app/Contents/MacOS"
          mkdir -p "aikun-manager.app/Contents/Resources"
          mv aikun-manager/* "aikun-manager.app/Contents/MacOS/"
          cat > "aikun-manager.app/Contents/Info.plist" << EOF
          <?xml version="1.0" encoding="UTF-8"?>
          <!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
          <plist version="1.0">
          <dict>
            <key>CFBundleExecutable</key>
            <string>aikun-manager</string>
            <key>CFBundleName</key>
            <string>Aikun Manager</string>
            <key>CFBundleIdentifier</key>
            <string>com.aikun.manager</string>
            <key>CFBundleVersion</key>
            <string>1.0.0</string>
            <key>CFBundlePackageType</key>
            <string>APPL</string>
          </dict>
          </plist>
          EOF
          # 打包成 zip
          zip -r "aikun-manager-macOS.zip" "aikun-manager.app"
      - uses: actions/upload-artifact@v4
        with:
          name: macos
          path: gui/dist/aikun-manager-macOS.zip

  build-linux:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Install dependencies
        run: pip install pyinstaller pywebview requests
      - name: Install system deps
        run: sudo apt-get install -y libgtk-3-dev libwebkit2gtk-4.1-dev
      - name: Build
        run: |
          cd gui
          pyinstaller --onefile --windowed --name "aikun-manager" main.py
      - uses: actions/upload-artifact@v4
        with:
          name: linux
          path: gui/dist/aikun-manager

  release:
    needs: [build-windows, build-macos, build-linux]
    runs-on: ubuntu-latest
    if: startsWith(github.ref, 'refs/tags/')
    permissions:
      contents: write
    steps:
      - uses: actions/download-artifact@v4
      - name: Create Release
        uses: softprops/action-gh-release@v2
        with:
          files: |
            windows/aikun-manager.exe
            macos/aikun-manager-macOS.zip
            linux/aikun-manager
          generate_release_notes: true
